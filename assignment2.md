# 📘 คู่มือปฏิบัติการ: โครงงานสถาปัตยกรรมคอมพิวเตอร์ (Comorg-Book-Thai)

**เอกสารประกอบการเรียนสำหรับนิสิตชั้นปีที่ 1 | การใช้งาน Git และ GitHub แบบครบวงจร**

เอกสารฉบับนี้จัดทำขึ้นเพื่อเป็นแนวทางปฏิบัติในการทำโครงงานแปลและเรียบเรียงหนังสือวิชาสถาปัตยกรรมคอมพิวเตอร์ ให้นิสิตอ่านและทำความเข้าใจไปทีละขั้นตอน เพื่อให้การทำงานกลุ่มเป็นไปอย่างราบรื่นและมีประสิทธิภาพ

---

## ส่วนที่ 1: ภาพรวมของโครงงาน (Project Overview)

**เป้าหมาย:** จัดทำหนังสือวิชาสถาปัตยกรรมคอมพิวเตอร์ฉบับภาษาไทย ผ่านระบบควบคุมเวอร์ชัน (Version Control)

**ขั้นตอนและเครื่องมือ:**

1. **การแบ่งงาน:** แบ่งเนื้อหาจากไฟล์ต้นฉบับ (PDF) ให้สมาชิกแต่ละคนรับผิดชอบตามหัวข้อย่อย
2. **การแปลงไฟล์เอกสาร:** แนะนำให้ใช้เครื่องมือประเภท `pdftotext` (Xpdf) เพื่อประสิทธิภาพสูงสุดในการดึงข้อความ (Text) ออกจากเอกสาร PDF
3. **การแปลและเรียบเรียง:** นำข้อความที่สกัดได้ มาแปลและเรียบเรียงเป็นภาษาทางการสำหรับหนังสือวิชาการ โดยจัดรูปแบบด้วยภาษา Markdown (นามสกุลไฟล์ `.md`)
4. **การจัดการผ่าน Git & GitHub:** สมาชิกทุกคนจะทำงานร่วมกันผ่าน Repository กลางบนอินเทอร์เน็ตที่ชื่อว่า `comorg-book-thai`

---

## ส่วนที่ 2: ปูพื้นฐาน Git และ GitHub

### ทำความเข้าใจพื้นฐานสำคัญก่อนเริ่มใช้ Git และ GitHub

* **Git:** คือ "ระบบ" ควบคุมเวอร์ชันที่ติดตั้งในคอมพิวเตอร์ของเรา ใช้สำหรับบันทึก, ติดตาม, และย้อนกลับประวัติการเปลี่ยนแปลงไฟล์ เช่นเดียวกับการเซฟเกมที่สามารถย้อนกลับหรือก้าวหน้าได้ตามจุดเซฟเดิม
* **GitHub:** คือ "เว็บไซต์" ที่ให้บริการเก็บข้อมูลและประวัติจาก Git ไว้บนอินเทอร์เน็ต ทำให้เราสามารถทำงานร่วมกับผู้อื่น แชร์ไฟล์ และสำรองข้อมูลได้ทุกที่ เปรียบเหมือนคลังข้อมูลออนไลน์สำหรับโปรแกรมเมอร์

---

### A. การเริ่มต้นใช้งานครั้งแรก (Initial Git Setup)

**ขั้นนี้ “สำคัญมาก” เพราะเป็นการตั้งตัวตนของเรากับ Git และ GitHub (ต้องทำใน “ทุกเครื่อง” ที่ใช้)**

1. **ติดตั้ง Git:**
   - [ดาวน์โหลด Git](https://git-scm.com/downloads) เลือกตามระบบปฏิบัติการของแต่ละคน (Windows/Mac/Linux)
   - หลังติดตั้ง ให้เปิด **Command Prompt** (Windows) หรือ **Terminal** (Mac/Linux)

2. **ตั้งค่า user name และ email (Global Config)**  
   - พิมพ์คำสั่งเหล่านี้ โดยใส่ชื่อ-นามสกุล หรือ Username และอีเมลที่ใช้กับ GitHub จริง
   - **ตัวอย่าง:**

```bash
git config --global user.name "Somchai S."  
git config --global user.email "somchai123@gmail.com"
```

> **หมายเหตุ:** การเซ็ต `--global` คือการตั้งค่าทั้งคอมพิวเตอร์ (ทุก Repo ในเครื่องจะใช้ค่านี้) ดูหลักการ [ย้อนกลับไปที่หัวข้อการตั้งค่าครั้งแรก](#a-การเริ่มต้นใช้งานครั้งแรก-initial-git-setup)

3. **ตรวจสอบค่าที่ตั้ง (Check Config):**  
   ดูค่าทุกอย่างที่ถูกกำหนดใน --global ด้วยคำสั่ง:

```bash
git config --global --list
```
   ถ้าอยากดูค่าเฉพาะในโปรเจคปัจจุบัน:

```bash
git config --list
```
   ถ้าอยากรู้แต่ละค่า (เช่น user.name):

```bash
git config user.name
git config --global user.name
```

---

### B. การแยกความหมาย Global / Local Config (และจะรู้ได้อย่างไร ?)

#### อธิบายลำดับและความแตกต่างระหว่าง Global กับ Local Config ของ Git ทีละขั้น

1. **ลำดับการอ่าน config ของ Git**
   - ทุกครั้งที่เราใช้ Git ในการทำงานกับ repository ใด ๆ Git จะตรวจสอบค่าการตั้งค่าต่าง ๆ (เช่น user.name, user.email) ว่าใช้อะไร
   - **Git จะเริ่มค้นหาค่านี้ในระดับ "local" ก่อน** (หมายถึงค่าที่ตั้งสำหรับ repository ปัจจุบันโดยเฉพาะ)
   - ถ้า **ยังไม่พบการตั้งค่านั้นใน local** Git จะ "fallback" หรือย้อนกลับไปหาค่าเดียวกันในระดับ "global" แทน

2. **Global Config คืออะไร**
   - **Global** หมายถึงการตั้งค่าที่มีผลกับ **ทุก repository** ในเครื่องคอมพิวเตอร์นี้
   - ตัวอย่างเช่น ถ้าเราตั้ง `user.name` และ `user.email` ใน global ด้วย `git config --global` ทุกครั้งที่สร้างหรือใช้งาน repo ใหม่ ๆ ที่ยังไม่ได้มี local config, Git จะใช้ค่าภายใต้ global นี้ให้อัตโนมัติ

3. **Local Config คืออะไร**
   - **Local** หมายถึงค่าการตั้งที่มีผล **เฉพาะ repository นั้นๆ** เท่านั้น
   - การตั้งค่าแบบ local นี้จะถูกบันทึกไว้ในโฟลเดอร์ `.git` ภายในโปรเจคของ repo แต่ละอัน
   - เวลาจะตั้งค่าระดับ local เราใช้คำสั่ง `git config` แบบไม่ใส่ `--global` (และต้องเปลี่ยน directory ไปภายใน repo นั้นก่อน)

4. **กรณีที่สองระดับซ้อนกัน (Override)**
   - หาก repository ใดตั้งค่า **ทั้ง global และ local** สำหรับ user.name หรือ config อื่น ๆ ผลลัพธ์สุดท้าย "Git จะเลือกใช้ค่าของ local" เท่านั้น
   - นั่นหมายความว่า config ที่เราตั้งไว้เฉพาะใน repo นี้ (local) จะมี priority สูงกว่าค่า global ทุกครั้ง

**สรุปสั้น ๆ:**  
- Local > Global  
- ถ้าตั้งค่าทั้งสองที่ Git จะใช้ค่าที่ตั้งไว้ใน local ของ repo นั้น ๆ เป็นหลักเสมอ  
- ถ้าไม่ตั้ง local, global ถึงจะถูกนำมาใช้  


**ตรวจสอบลำดับ config:**  
Git จะอ่านค่าตามลำดับนี้
1. Repository (local)  
2. Global (~/.gitconfig, บางทีอาจอยู่ที่ C:\Users\ชื่อของคุณ\.gitconfig)  
3. System

ดูว่าไฟล์ config อยู่ที่ไหนพิมพ์:
```bash
git config --list --show-origin
```
จะแสดงตำแหน่งไฟล์และค่าที่อ่านจริง

---

### C. จะเพิ่ม/สร้าง Repository ใหม่ หรือทำงานกับ repo หลายอันอย่างไร?

1. **เริ่มโปรเจคใหม่ในโฟลเดอร์ปัจจุบัน**
   ```bash
   git init
   ```
2. **หรือถ้าจะ clone repo จาก GitHub**  
   ให้ไปที่หน้า Repo บน GitHub กดปุ่ม “Code” แล้ว copy ลิงค์มารัน:
   ```bash
   git clone https://github.com/yourusername/myproject.git
   ```

3. **กำหนด Local Config เฉพาะ Repo**  
   (กรณีต้องการชื่อ/อีเมลหรือ user อื่นใน repo นี้)  
   ต้องเข้าไปในโฟลเดอร์โปรเจคนั้นก่อน แล้วพิมพ์:
   ```bash
   git config user.name "ชื่อ-นามสกุลใหม่"
   git config user.email "อีเมลใหม่@ตัวอย่าง.com"
   ```
   ดูค่าที่ตั้งไว้ (local):
   ```bash
   git config --list
   ```
   Node: จะมี 2 บรรทัด user.name และ user.email ที่แตกต่างจาก global ได้

---

### D. สลับหลายบัญชี GitHub/Git (กรณีหลาย userid หรือใช้การบ้านกับงานจริง)

**1. ตั้งค่าชื่อ/อีเมล เฉพาะ repo เลย (ตามหัวข้อก่อนหน้า C.3) ถ้าแต่ละ repo ใช้ user/email คนละตัว**

**2. กรณี push/pull ไปที่ GitHub ถ้ามีหลาย Account ในเครื่องเดียว:**
- ถ้าใช้ HTTPS เมื่อ push/pull Git จะถาม username/password ทุกครั้ง (ต่อไป GitHub จะให้ใช้ “Personal Access Token” แทน password [อ่านวิธีที่นี่](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token))
- ถ้าใช้ SSH (แนะนำสำหรับ advanced) สามารถ generate key หลายอัน (ดูวิธี [Setting up SSH keys for multiple GitHub accounts](https://docs.github.com/en/authentication/connecting-to-github-with-ssh/managing-ssh-keys))  
- สำหรับงานแต่ละชุด, คุณต้อง set remote url เป็น ssh ที่ต่างกัน ตัวอย่าง:
  ```bash
  git remote set-url origin git@github.com:username1/repo1.git
  git remote set-url origin git@github.com:username2/repo2.git
  ```
- รายละเอียดเพิ่มให้ดู:  
  [ย้อนกลับไปเรื่อง local config](#b-การแยกความหมาย-global--local-config-และจะรู้ได้อย่างไร-)

**3. เช็คว่าระบบอ่าน config ตัวไหนอยู่**
```bash
git config --list --show-origin
```
จะเห็นชัดๆ config แต่ละตัวมาจากไฟล์ไหน (global, local, system)

**4. สลับ Account สำหรับ GitHub Desktop หรือ CLI**
- หากใช้ GitHub Desktop สามารถสลับ user ได้ใน app
- หากใช้ CLI ขึ้นกับการตั้งชื่อ/อีเมล และ remote ที่กำหนดไว้ใน repo

---

### E. วิธีแก้ไข/เปลี่ยนแปลงค่าที่ตั้งไว้

- เปลี่ยนค่า global:
  ```bash
  git config --global user.name "ชื่อใหม่"
  git config --global user.email "อีเมลใหม่"
  ```
- ลบค่า global:
  ```bash
  git config --global --unset user.name
  git config --global --unset user.email
  ```
- เปลี่ยนหรือลบค่า local (ใน repo ปัจจุบัน):
  ```bash
  git config --unset user.name
  git config --unset user.email
  ```

> ถ้าค่า local ถูกลบ Git จะย้อนกลับไปใช้ค่า global อัตโนมัติ ([อ้างอิงหลักการดูหัวข้อ B](#b-การแยกความหมาย-global--local-config-และจะรู้ได้อย่างไร-))

---

> 💡 **สรุปสำคัญ**
> - ใช้ Git ในเครื่องไหนก็ต้องตั้งชื่อ อีเมล (ดู [Initial Setup](#a-การเริ่มต้นใช้งานครั้งแรก-initial-git-setup))
> - ถ้าทำหลายโปรเจค/หลายบัญชี ตั้งค่าเฉพาะ repo ได้ (ดู [Local Config](#b-การแยกความหมาย-global--local-config-และจะรู้ได้อย่างไร-))
> - ใช้ `git config --list --show-origin` เพื่อตรวจสอบค่าจริง
> - เพิ่ม repo ใหม่ด้วย git init หรือ git clone ได้ทุกโฟลเดอร์

---

**ดูคำอธิบายเพิ่มเติมในคู่มือของ GitHub**  
- [Pro Git Book ภาษาไทย](https://git-scm.com/book/th/v2)
- [Getting started with GitHub](https://docs.github.com/en/get-started)

[กลับไปอ่านเรื่องภาพรวมโครงงาน](#ส่วนที่-1-ภาพรวมของโครงงาน-project-overview)
[ข้ามไปดู workshop ตัวอย่างการใช้งานจริง](#ส่วนที่-3-บททดสอบก่อนลงสนามจริง-มินิเวิร์กชอป-15-นาที)

   - คำว่า `--global` หมายความว่าจะใช้ค่าที่ตั้งนี้กับทุก Repository ที่เราทำงานในเครื่องนี้ (ถ้าต้องการเปลี่ยนเฉพาะ repo ใด repo หนึ่ง ให้เอา `--global` ออก)
   - สามารถตรวจสอบผลการตั้งค่าได้ด้วยคำสั่งนี้:

```bash
git config --global --list
```

---

### การใช้งาน Git กับหลายอุปกรณ์ (เช่น ใช้ที่บ้าน กับบนโน้ตบุ๊ค)

- ทุกเครื่องที่ต้องการใช้งาน Git เชื่อมกับบัญชี GitHub ของเรา **ต้องติดตั้ง Git** และทำขั้นตอนตั้งค่าผู้ใช้ (`git config --global`...) ให้ครบถ้วนทุกเครื่อง
- เวลาย้ายไปใช้เครื่องใหม่ (เช่น เครื่องมหาวิทยาลัยหรือโน้ตบุ๊ค) ต้องตั้งค่า `user.name` และ `user.email` ใหม่ทุกครั้ง ไม่งั้นจะ commit ไม่สำเร็จ หรือ commit ขึ้นชื่อเก่าได้

**สำหรับการเชื่อมต่อกับ GitHub:**
- ต้องเข้าสู่ระบบ GitHub ทางเว็บ (browser) ทุกครั้งที่จะส่งงานหรือใช้งานแบบออนไลน์
- หากใช้การ push/pull ครั้งแรกในเครื่องใหม่ ระบบจะถาม username/password ของ GitHub (หรือถ้าตั้งค่า Two-Factor Authentication จะต้องใช้ Personal Access Token แทนรหัสผ่าน — อ่านรายละเอียดในคู่มือ)
- แนะนำให้ตั้งค่า SSH Key หากต้องการเชื่อมต่อโดยไม่ต้องกรอกรหัสทุกครั้ง 

---

💡 **สรุป Key Point ที่ห้ามลืม:**
- ใช้ Git ครั้งแรกในเครื่องใด ต้อง config ชื่อกับอีเมลก่อนเสมอ
- แต่ละเครื่องต้องตั้งค่าของใครของมัน ไม่ใช่ตั้งครั้งเดียวใช้ได้ทุกที่
- ระวังอย่าสลับอีเมล (ควรใช้อีเมลเดียวกับที่สมัคร GitHub เพื่อง่ายต่อการเชื่อมโยงและแสดงผล profile ในเว็บ)

หากต้องการ reset หรือเปลี่ยนค่าที่ตั้งไว้ สามารถใช้คำสั่ง:

```bash
git config --global --unset user.name
git config --global --unset user.email
```

แล้ว config ใหม่ตามขั้นตอนด้านบนอีกครั้ง


---

## ส่วนที่ 3: บททดสอบก่อนลงสนามจริง (มินิเวิร์กชอป 15 นาที)

เพื่อป้องกันความผิดพลาดในโครงงานจริง ให้นิสิตทำภารกิจ **"สมุดลงนามผู้รอดชีวิต"** เพื่อจำลองการทำงานเป็นทีมก่อน 

**กติกาสำคัญ:** ห้ามแก้ไขไฟล์เดียวกันเด็ดขาด ให้สร้างไฟล์เป็นชื่อเล่นของตัวเองเท่านั้น

**🎬 หน้าที่ของ "หัวหน้ากลุ่ม" (ผู้สร้างฐานทัพ):**

1. เข้าเว็บ GitHub สร้าง Repository ใหม่ชื่อ `git-workshop-101`
2. ไปที่เมนู **Settings > Collaborators** ใส่อีเมลเชิญเพื่อนทุกคนในกลุ่ม
3. เปิด Command Prompt สร้างโครงสร้างเริ่มต้นและส่งขึ้นเว็บตามคำสั่งด้านล่างนี้:

```bash
git clone [https://github.com/ช](https://github.com/ช)ื่อหัวหน้า/git-workshop-101.git
cd git-workshop-101
mkdir members
git add .
git commit -m "หัวหน้าสร้างโฟลเดอร์ members รอแล้วจ้า"
git push origin main
```

**🏃‍♂️ หน้าที่ของ "ลูกทีมทุกคน" (รวมถึงหัวหน้าด้วย):**

1. กดยอมรับคำเชิญในอีเมล แล้วดึงฐานทัพลงมาที่เครื่องตัวเอง:

```bash
git clone [https://github.com/ช](https://github.com/ช)ื่อหัวหน้า/git-workshop-101.git
cd git-workshop-101
```

2. แตกสาขา (Branch) ส่วนตัว โดยตั้งชื่อเป็นชื่อเล่นตัวเองภาษาอังกฤษ:

```bash
git checkout -b name-somchai
```

3. เข้าไปในโฟลเดอร์ `members` สร้างไฟล์ใหม่ชื่อ `somchai.md` พิมพ์ข้อความทักทายข้างใน แล้วกด Save
4. ส่งแผ่นป้ายชื่อขึ้นเว็บ:

```bash
git add .
git commit -m "ส่งป้ายชื่อของสมชาย"
git push -u origin name-somchai
```

**🤝 การตรวจรับงาน (ดำเนินการบนเว็บ GitHub):**

1. **ลูกทีม:** เข้าเว็บ GitHub ไปที่ Repository ของกลุ่ม กดปุ่มสีเขียว **Compare & pull request** และกด **Create pull request**
2. **หัวหน้ากลุ่ม:** ไปที่แท็บ **Pull requests** ตรวจสอบงานเพื่อน แล้วกดปุ่ม **Merge pull request** สีเขียว เพื่อรวมไฟล์
3. **ทุกคน:** สลับกลับไปสาขาหลักและดึงข้อมูลที่รวมแล้วลงเครื่อง:

```bash
git checkout main
git pull origin main
```
*(เมื่อเปิดโฟลเดอร์ members ดู จะพบไฟล์ของเพื่อนทุกคนมารวมกันอย่างสมบูรณ์)*

---

## ส่วนที่ 4: การทำงานโครงงานจริง (Comorg-Book-Thai)

เมื่อเข้าใจระบบจากการทำเวิร์กชอปแล้ว เราจะนำมาใช้กับโครงงานจริง

**4.1 การเตรียมโครงสร้างโดย "หัวหน้ากลุ่ม" (Project Skeleton)**

หัวหน้าต้องสร้าง Repository `comorg-book-thai` และสร้างโครงสร้างโฟลเดอร์ทิ้งไว้ เพื่อไม่ให้เกิด Error โฟลเดอร์ว่าง ดังนี้:

**สร้างไฟล์ `README.md` (หน้าปกหลัก) ไว้หน้าสุด:**

```markdown
# สารบัญหลัก
* [บทที่ 4: หน่วยความจำ (Memory)](chapter-4/README.md)
```

**สร้างไฟล์ `chapter-4/README.md` (สารบัญย่อยประจำบท) ไว้ในโฟลเดอร์:**

```markdown
# บทที่ 4: การจัดการหน่วยความจำ
| หัวข้อ | ผู้รับผิดชอบ | ลิงก์ไฟล์ |
| :--- | :--- | :--- |
| 4.1 | นางสาว ก. | [คลิกอ่าน 4.1](4-1.md) |
| 4.2 | นาย ข. | [คลิกอ่าน 4.2](4-2.md) |
```

**4.2 กฎเหล็กการตั้งชื่อไฟล์และโฟลเดอร์ ⚠️**

เพื่อป้องกันปัญหาลิงก์พังบน GitHub ให้นิสิตยึดกติกาดังนี้อย่างเคร่งครัด:

* **ห้ามใช้ภาษาไทย:** ให้ใช้ภาษาอังกฤษเท่านั้น (❌ ห้าม: `บทที่4.md` | ✅ ถูกต้อง: `chapter-4.md`)
* **ห้ามเว้นวรรค:** ให้ใช้ขีดกลางแทน (❌ ห้าม: `chapter 4.md` | ✅ ถูกต้อง: `chapter-4.md`)
* **ใช้ตัวพิมพ์เล็กทั้งหมด:** เพื่อป้องกันปัญหาการอ่านชื่อไฟล์ตัวพิมพ์เล็ก-ใหญ่ไม่ตรงกัน (ยกเว้นไฟล์ `README.md` ที่ต้องพิมพ์ใหญ่เสมอ)

**4.3 ขั้นตอนการปฏิบัติงานของ "ลูกทีม"**

1. **ดึงข้อมูลล่าสุด (Pull):** เพื่อให้ได้โครงสร้างอัปเดตจากหัวหน้า
```bash
git checkout main
git pull origin main
```
2. **แตกสาขา (Branch):** สร้างพื้นที่ทำงานส่วนตัว
```bash
git checkout -b section-4.1
```
3. **ทำงาน:** สร้างไฟล์ `4-1.md` ในโฟลเดอร์ `chapter-4` แล้วพิมพ์เนื้อหา
4. **ส่งงาน (Push):** บันทึกและส่งขึ้นไปรอบนคลาวด์
```bash
git add .
git commit -m "เพิ่มหัวข้อ 4.1"
git push -u origin section-4.1
```
5. **ขอรวมไฟล์ (Pull Request):** เข้าเว็บ GitHub ไปกด Create Pull Request ให้หัวหน้ากลุ่มกด Merge

---

## ส่วนที่ 5: การทำงานหลายเครื่อง (Git Syncing)

หากนิสิตต้องทำงานสลับเครื่อง (เช่น คอมพิวเตอร์หอพัก สลับกับ โน้ตบุ๊กพกพา) กฎเหล็กมีเพียงข้อเดียวคือ: **"ดึงงานลงมาก่อนเริ่มทำ และส่งงานขึ้นไปทุกครั้งหลังทำเสร็จ"**

**1. ก่อนลุกจากเครื่อง A (ส่งงานขึ้นคลาวด์):**

```bash
git add .
git commit -m "อัปเดตเนื้อหาจากคอมหอพัก"
git push origin [ระบุชื่อสาขาที่กำลังทำ]
```

**2. เมื่อมาเปิดเครื่อง B (ดึงงานลงมาอัปเดต):**

ห้ามเริ่มพิมพ์งานทันที! ให้ดึงงานล่าสุดก่อนเสมอ

```bash
git pull origin [ระบุชื่อสาขาที่กำลังทำ]
```
*(เมื่อพิมพ์งานที่เครื่อง B เสร็จ ก็ให้วนลูปกลับไปทำข้อ 1 ใหม่อีกครั้ง)*

---

## ส่วนที่ 6: การสร้างเว็บไซต์จากโครงงานด้วย GitHub Pages (สำหรับนำเสนอ)

เพื่อให้การนำเสนอผลงานเป็นไปอย่างมืออาชีพ GitHub มีบริการแปลงไฟล์ Markdown (`.md`) ทั้งหมดใน Repository ให้กลายเป็นหน้าเว็บไซต์ที่สวยงามและอ่านง่ายโดยอัตโนมัติ



**ขั้นตอนการเปิดใช้งาน (ดำเนินการโดยหัวหน้ากลุ่มบนเว็บ GitHub):**

1. เข้าไปที่หน้า Repository หลักของกลุ่ม
2. คลิกที่แท็บ **Settings** (สัญลักษณ์รูปฟันเฟืองด้านบนขวา)
3. ที่เมนูด้านซ้ายมือ ให้เลื่อนหาและคลิกที่คำว่า **Pages**
4. ในหัวข้อ **Build and deployment** > **Source** ให้เลือกเป็น **Deploy from a branch**
5. ในส่วนของ **Branch** ให้คลิกเปลี่ยนจาก `None` เป็น `main` และเลือกโฟลเดอร์ `/ (root)` จากนั้นกดปุ่ม **Save**
6. รอระบบประมวลผลประมาณ 1-2 นาที แล้วกดรีเฟรชหน้าเว็บ จะปรากฏลิงก์เว็บไซต์ของกลุ่มขึ้นมา (ตัวอย่าง: `https://ชื่อยูสเซอร์.github.io/comorg-book-thai/`) สามารถนำลิงก์นี้ส่งอาจารย์ได้เลย

*(หมายเหตุ: หากนิสิตทุกคนทำตามกฎการตั้งชื่อไฟล์ในหัวข้อ 4.2 อย่างเคร่งครัด ลิงก์สารบัญต่างๆ บนหน้าเว็บไซต์จะสามารถคลิกเชื่อมโยงกันได้อย่างสมบูรณ์แบบโดยไม่ต้องเขียนโค้ดเพิ่มเติม)*

---

## 🎯 สรุปคำสั่งที่ใช้บ่อยที่สุดในชีวิตประจำวัน (Cheat Sheet)

วงจรการทำงานปกติของคุณจะวนอยู่แค่ 4 คำสั่งนี้:

1. `git pull` : ดึงงานล่าสุดจากเว็บลงมา (ทำก่อนเริ่มงานเสมอ)
2. `git add .` : นำไฟล์ที่แก้ไขทั้งหมดเข้าคิวเตรียมเซฟ
3. `git commit -m "อธิบายงานที่ทำ..."` : เซฟประวัติและอธิบายสิ่งที่ทำ
4. `git push` : ส่งงานขึ้นเว็บ

---
# 📘 ภาคผนวก: กรณีศึกษาปัญหาที่พบบ่อยในการใช้งาน Git และ GitHub

เอกสารฉบับนี้จัดทำขึ้นเพื่ออธิบายกรณีศึกษาปัญหาที่เกิดขึ้นจริงระหว่างการใช้งาน Git และ GitHub โดยเฉพาะกรณีที่นิสิตใช้หลายบัญชี GitHub ในเครื่องเดียว หรือทำงานหลายเครื่อง และพบปัญหาไม่สามารถ `push` งานได้  

เนื้อหานี้เหมาะสำหรับนิสิตชั้นปีที่ 1 เพื่อทำความเข้าใจปัญหาเชิงระบบ และแก้ไขได้อย่างถูกต้องตามหลักวิชา

---

# กรณีที่ 1: Push ไม่ได้ (Permission Denied / 403)

## อาการที่พบ

เมื่อพิมพ์คำสั่ง

```bash
git push
```

ระบบแสดงข้อความลักษณะดังนี้

```
remote: Permission denied
fatal: unable to access ...
```

## สาเหตุ

เครื่องกำลังยืนยันตัวตนด้วยบัญชี GitHub ที่ไม่มีสิทธิ์เขียน (write access) ใน Repository นั้น

> หมายเหตุ:  
> การตั้งค่า `user.name` และ `user.email` ไม่มีผลต่อสิทธิ์ในการ push  
> สิทธิ์ push ขึ้นอยู่กับการ “ยืนยันตัวตน” (Authentication) เท่านั้น

---

# แนวทางแก้ไขที่ถูกต้อง: ใช้ SSH Key แยกตามบัญชี

หากมีหลายบัญชี GitHub อยู่ในเครื่องเดียว  
**ไม่แนะนำให้ใช้การเชื่อมต่อแบบ https** เพราะ https จะผูกกับบัญชีผู้ใช้แบบ global ในเครื่องนั้น ซึ่งแต่ละครั้งที่ push หรือ pull จะถูกถาม username/password หรือ personal access token เสมอ ทำให้ยุ่งยากและอาจเกิดความสับสนระหว่างบัญชีต่าง ๆ

**ข้อดีของการใช้ SSH Key:** SSH ช่วยให้สามารถกำหนด identity แยกเฉพาะแต่ละบัญชี ด้วยการสร้างไฟล์ key คนละคู่และตั้งค่าใน `~/.ssh/config` เมื่อทำงานกับ repo ไหน ก็เลือกใช้ SSH Key ของบัญชีนั้น ๆ ได้อย่างอัตโนมัติ

ดังนั้น ถ้ามีหลายบัญชี GitHub บนเครื่องเดียว ควรใช้ SSH Key แยกกัน เพราะ
- ไม่ต้องสลับ login/logout หรือพิมพ์รหัสผ่านทุกครั้งแบบ https
- จัดการสิทธิ์ชัดเจนและปลอดภัยกว่า
- ง่ายต่อการบริหารหลายโปรไฟล์

**สรุป:**  
- https สะดวกเฉพาะกรณีใช้ “บัญชีเดียว”
- ถ้ามี “หลายบัญชี” ต้องใช้ SSH เท่านั้น และแยก key สำหรับแต่ละบัญชีเพื่อป้องกันปัญหา push/pull สลับกัน

## ขั้นตอน

### 1. สร้าง SSH Key แยกตามบัญชี

```bash
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_account1 -C "account1"
ssh-keygen -t ed25519 -f ~/.ssh/id_ed25519_account2 -C "account2"
```

### 2. นำไฟล์ `.pub` ไปเพิ่มใน GitHub

- เข้า GitHub
- ไปที่ Settings → SSH and GPG keys
- กด New SSH key
- วาง key จากไฟล์ `.pub`

### 3. สร้างไฟล์ `~/.ssh/config`

```ssh
Host github-account1
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_account1
  IdentitiesOnly yes

Host github-account2
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_account2
  IdentitiesOnly yes
```

### 4. ตั้งค่า remote ให้ถูกบัญชี

```bash
git remote set-url origin git@github-account1:account1/repo.git
```

---

# กรณีที่ 2: Repository not found

## อาการที่พบ

```
ERROR: Repository not found.
```

## ตรวจสอบ

```bash
git remote -v
```

หากพบว่า remote เป็น

```
git@github-account:OWNER/REPO.git
```

แสดงว่ายังไม่ได้ตั้งชื่อ Repository จริง

## วิธีแก้

```bash
git remote set-url origin git@github-account:username/repository-name.git
```

ตรวจสอบอีกครั้ง

```bash
git remote -v
```

---

# กรณีที่ 3: Push ถูกปฏิเสธ (fetch first)

## อาการที่พบ

```
! [rejected] main -> main (fetch first)
```

## ความหมาย

บน GitHub มี commit ใหม่ที่เครื่องเรายังไม่มี  
Git ป้องกันไม่ให้ทับงานผู้อื่น

## วิธีแก้ที่ถูกต้อง

```bash
git pull --rebase origin main
git push
```

---

## หากเกิด Conflict

1. เปิดไฟล์ที่มีเครื่องหมาย

สัญลักษณ์เหล่านี้คือ "conflict marker" ที่ Git ใส่เข้ามาในไฟล์เพื่อบอกว่ามีข้อขัดแย้ง (conflict) ระหว่างเนื้อหาจากสองแหล่งในช่วงการ merge หรือ rebase

- `<<<<<<<` คือจุดเริ่มต้นของเนื้อหาจากฝั่งปัจจุบัน (HEAD)
- `=======` คือเส้นแบ่งระหว่างสองฝั่ง
- `>>>>>>>` คือจุดสิ้นสุดของเนื้อหาจากฝั่งที่จะรวมเข้ามา (เช่น branch ใหม่ที่เรา pull/rebase เข้ามา)

**วิธีการดู:**  
ให้เปิดไฟล์ที่มี conflict ใน editor จะเห็นบรรทัดที่ขึ้นต้นด้วยเครื่องหมายเหล่านี้  
เนื้อหาระหว่าง `<<<<<<<` และ `=======` คือของฝั่งเรา  
เนื้อหาระหว่าง `=======` และ `>>>>>>>` คือของฝั่งที่เข้ามาใหม่  
หน้าที่ของเราคือเลือกหรือแก้ไขให้เนื้อหาถูกต้อง แล้วลบเครื่องหมายเหล่านี้ออกให้หมด ก่อนดำเนินการขั้นตอนถัดไป

ตัวอย่าง:

```
<<<<<<< HEAD
เนื้อหาของเรา
=======
เนื้อหาจาก branch ที่ merge/rebase เข้ามา
>>>>>>> branch-name
```

2. แก้ไขให้ถูกต้อง  
   - อ่านเนื้อหาทั้งสองฝั่ง (ระหว่าง `<<<<<<<` กับ `=======` และระหว่าง `=======` กับ `>>>>>>>`)
   - ตัดสินใจว่าจะเลือกเนื้อหาฝั่งใด หรือจะรวมเนื้อหาทั้งสองแบบ เพื่อให้ไฟล์มีเนื้อหาที่ถูกต้องที่สุด
   - ลบเครื่องหมาย conflict marker (`<<<<<<<`, `=======`, `>>>>>>>`) ออกให้หมด
   - ตรวจทานไฟล์หลังแก้ไขอีกครั้ง เพื่อให้มั่นใจว่าเนื้อหาถูกต้องและไม่มี conflict marker เหลืออยู่
3. บันทึกไฟล์ แล้วพิมพ์

```bash
git add .
# เมื่อแก้ conflict และ git add ไฟล์ที่มีปัญหาแล้ว ต้องสั่ง
git rebase --continue
# ความหมาย:
# ในกระบวนการ rebase เมื่อเกิด conflict Git จะหยุดเพื่อให้เราแก้ไขไฟล์ก่อน
# หลังจากแก้และบันทึกไฟล์ แล้ว add ด้วย git add . ให้ใช้ "git rebase --continue"
# เพื่อบอก Git ว่าเราแก้ปัญหาแล้ว ให้ดำเนิน rebase ต่อไปจนจบขั้นตอน
```

---

# กรณีที่ 4: ทำงานหลายเครื่อง

หลักการสำคัญมีเพียงข้อเดียว

> ก่อนเริ่มงาน → `git pull`  
> หลังทำงานเสร็จ → `git push`

ห้ามเริ่มพิมพ์งานทันทีโดยไม่ดึงงานล่าสุดก่อน

---

# แนวคิดสำคัญที่ต้องเข้าใจ

ปัญหา Git ส่วนใหญ่เกี่ยวข้องกับ 3 ประเด็นหลัก

## 1. Authentication  
ใครคือคนที่กำลัง push อยู่

## 2. Authorization  
บัญชีนั้นมีสิทธิ์ใน Repository หรือไม่

## 3. Synchronization  
ประวัติในเครื่องกับบน GitHub ตรงกันหรือไม่

หากเข้าใจ 3 ประเด็นนี้ จะสามารถวิเคราะห์ปัญหาได้อย่างเป็นระบบ

---

# Checklist ก่อนสั่ง `git push`

- ตรวจสอบ remote ด้วย `git remote -v`
- ตรวจสอบบัญชี SSH ด้วย `ssh -T github-ชื่อบัญชี`
- ดึงงานล่าสุดด้วย `git pull`
- ตรวจสอบ branch ด้วย `git branch`

---

# สรุปสาระสำคัญ

- ปัญหา push ไม่ได้ ส่วนใหญ่เกิดจากการตั้งค่าผิด
- การแยก SSH key ต่อบัญชีเป็นวิธีที่ปลอดภัยและถูกต้องที่สุด
- ห้ามใช้ `git push --force` โดยไม่เข้าใจผลกระทบ
- Git ทำงานตามหลักตรรกะ ไม่ได้เกิดข้อผิดพลาดแบบสุ่ม

---

หากนิสิตสามารถวิเคราะห์และแก้ปัญหาตามขั้นตอนข้างต้นได้  
ถือว่ามีความเข้าใจระบบ Version Control ในระดับพร้อมทำโครงงานจริงแล้ว